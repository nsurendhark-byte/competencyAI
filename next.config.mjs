/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/competencyAI',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;